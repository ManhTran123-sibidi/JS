// Toán tử
// kiểm tra hoặc thay đổi giá trị của biến
// phép gán
// phép toán toán học
// phép so sánh
// phép logic
// phép ternary

var HP;
HP = 100;

//+ - * / % ** math.sprt
HP = HP + 10; // 110
HP = HP % 100; // 10
// Hp % 2 
HP = HP ** 2; // 100

//+= -= *= /=
HP -=20; //hp = hp -20 // 80

// ++ -- 
HP++; // HP = HP +1 // 81

// HP = (HP * 2) + (HP / 5);

var heloo = "hello"
var world = "world"
var result = hello + world;

// so sánh > >= < <= == != ===
var n1 = 5;
var n2 = 10;
result = m1 > n2; //false=))
result = n1 != n2; //true

result = hello == world; // false

//logic: ! , || , && 
result = !result; // true
result = n1 > n2 && hello == world; // chỉ đúng khi cả 2 đều đúng.
n2 = n1;
result = n1 == n2 || hello == world; // đúng nếu 1 trong 2 đúng và ngược lại

// ? : 
result = m1 > n2 ? n1 : n2;// nếu n1 > n2 là đúng thì n1 đc gán cho result còn nếu n1 < n2 thì n2 đc gán cho result.


