// câu lệnh if
//kiểm tra => đúng => chạy đoạn code phía dưới
// else - if => kiểm tra tiếp theo 
//else 
// câu lệnh if lồng và thực hiện các toán tử logic

var keyPressed = "w"
var vi_tri = 5;
var HP = 10;
var vi_tri_gioi_han = 50;


if(HP > 0); {

}
    if (keyPressed = "w" && vi_tri < vi_tri_gioi_han) {
    vi_tri++;

    } else if (keyPressed =="s" ) {
    vi_tri--;
    } else {
    vi_tri = 0;
}
